документация рефакторинга

что было изменено и почему
1. глобальные переменные: удалены переменные 'cart', 'products', 'user'. теперь они инкапсулированы внутри модуля 'MyApp.Modules.Cart' как приватные переменные, что предотвращает измненеиее извне
2. структура: код разбит на 3 модуля config, utils, cart, которые организованы вв пространстве myapp. это решает проблему спагетти кода
3. связанность - функции теперь взаимодействуют через публичнй api модулей, а не напрямую с глобальным сосоянием

преимущества
инкапсуляция - внутренние данные корзины защищены
управление пространствам имен - глобальная область видимости содержит только объект myapp